---
name: cli-resilience
description: Inspect and manage circuit-breaker states, connection cooldowns, quota limits, and backoff levels from the CLI. Reset stuck providers and configure resilience thresholds.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect and manage circuit-breaker states, connection cooldowns, quota limits, and backoff levels from the CLI. Reset stuck providers and configure resilience thresholds.

## Quick install

```bash
npm install -g nextroute   # or: npx nextroute
nextroute --version
```

## Subcommands

### `quota`

**Flags:**

- `--provider <id>`
- `--json`

**Example:**

```bash
nextroute quota
```

### `resilience`

**Example:**

```bash
nextroute resilience
```

### `resilience status`

**Flags:**

- `--provider <p>`

**Example:**

```bash
nextroute resilience status
```

### `resilience breakers`

**Flags:**

- `--provider <p>`

**Example:**

```bash
nextroute resilience breakers
```

### `resilience cooldowns`

**Flags:**

- `--provider <p>`
- `--connection-id <id>`

**Example:**

```bash
nextroute resilience cooldowns
```

### `resilience lockouts`

**Flags:**

- `--provider <p>`
- `--model <m>`

**Example:**

```bash
nextroute resilience lockouts
```

### `resilience reset`

**Flags:**

- `--connection-id <id>`
- `--model <m>`
- `--all-cooldowns`
- `--yes`

**Example:**

```bash
nextroute resilience reset
```

### `resilience profile`

**Example:**

```bash
nextroute resilience profile
```

### `resilience show`

**Example:**

```bash
nextroute resilience show
```

### `resilience set`

**Example:**

```bash
nextroute resilience set
```

### `resilience config`

**Example:**

```bash
nextroute resilience config
```

### `resilience show`

**Example:**

```bash
nextroute resilience show
```

### `resilience set`

**Flags:**

- `--threshold <n>`
- `--reset-timeout <ms>`
- `--base-cooldown <ms>`

**Example:**

```bash
nextroute resilience set
```
